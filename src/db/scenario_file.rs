// CRUD now provided generically via the `db::FileStore` trait impl on
// `scenario_file::Model` (see file_store.rs). Module retained empty
// for symmetry with `db::map_file` and to keep room for any future
// scenario-file-specific helpers.
